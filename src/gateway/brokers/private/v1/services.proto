syntax = "proto3";

package gateway.brokers.private.v1;

import "gateway/brokers/private/v1/procedures.proto";

option go_package = "gitlab.com/evemeta/zdk/pb/out/go/gateway/brokers/private/v1;brokerspb";

service Service {

    rpc Broadcast(BroadcastRequest) returns (BroadcastResponse);

    rpc InitiateBroadcastMutations(BroadcastTransaction) returns (BroadcastTransaction);

    rpc FinalizeBroadcastMutations(BroadcastTransaction) returns (BroadcastTransaction);

}