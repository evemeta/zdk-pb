syntax = "proto3";

package gateway.jobs.private.v1;

import "gateway/jobs/private/v1/enums.proto";
import "gateway/jobs/private/v1/entities.proto";

option go_package = "gitlab.com/evemeta/zdk/pb/out/go/gateway/jobs/private/v1;jobspb";

// OptionalMap todo;
message OptionalMap {
    map<string, string> value = 1;
}

// OptionalResult todo;
message OptionalResult {
    gateway.jobs.private.v1.Result value = 1;
}

// OptionalStatus todo;
message OptionalStatus {
    gateway.jobs.private.v1.Status value = 1;
}

// OptionalString represents a string value that can be intentionally left unset or null.
// It allows for explicit control over the presence or absence of the string, accommodating scenarios where the string might be optional or intentionally omitted.
message OptionalString {
    string value = 1;
}